name: mind_arena
description: "Mind Arena - Quiz Contest App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔥 REQUIRED PACKAGES
  http: ^1.2.0
  provider: ^6.0.5

  # 🔥 OPTIONAL (Future use - recommended)
  shared_preferences: ^2.2.2
  image_picker: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # 🔥 Agar screenshots upload karni hain future me
 # assets:
   # - assets/images/